{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "compile and run (global I/O)",
      "type": "shell",
      "command": "g++ -std=c++17 -o ${fileBasenameNoExtension} ${file} && ./\"${fileBasenameNoExtension}\" < /home/satyam/DSA/input.txt > /home/satyam/DSA/output.txt",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "options": {
        "cwd": "${fileDirname}"
      },
      "problemMatcher": []
    }
  ]
}
